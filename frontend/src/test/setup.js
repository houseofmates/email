// vitest global setup — runs before each test file.
// adds @testing-library/jest-dom matchers (toBeInTheDocument, etc.) for the
// component tests we'll add as the verticals land.
import "@testing-library/jest-dom/vitest"
